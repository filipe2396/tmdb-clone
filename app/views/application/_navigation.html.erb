<nav class="navbar navbar-expand-md navbar-dark bg-primary mb-4">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-collapse">
      <span class="navbar-toggler-icon"></span>
    </button>

    <a class="navbar-brand me-0 me-md-3" href="/">TMDB Clone</a>

    <ul class="navbar-nav flex-row order-md-1">
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle p-2" href="#" role="button" data-bs-toggle="dropdown">
            <%= icon('fas', 'user') %>
          </a>

          <ul class="dropdown-menu dropdown-menu-end position-absolute">
            <%= link_to t('.favorites'), favorites_path, class: 'dropdown-item' %>
            <hr class="dropdown-divider">
            <%= link_to t('.logout'), destroy_user_session_path, class: 'dropdown-item' %>
          </ul>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to new_user_session_path, class: 'nav-link p-2' do %>
            <%= icon('fas', 'user') %>
          <% end %>
        </li>
      <% end %>
    </ul>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= active_link_to t('.search'), search_path,
            active: controller_name == 'searches',
            class: 'nav-link' %>
        </li>

        <li class="nav-item">
          <%= active_link_to t('.local_movies'), local_movies_path,
            active: controller_name == 'local_movies',
            class: "nav-link" %>
        </li>
      </ul>
    </div>
  </div>
</nav>
